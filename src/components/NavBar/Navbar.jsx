import { Button, Container } from "@mui/material";

import React from "react";

export const Navbar = () => {
  return (
    <Container>
      <h2>Barra de navegacion</h2>
      <Button variant="contained" maxWidth="xs">
        Hello World
      </Button>
    </Container>
  );
};
